{
  "ConnectionStrings": {
    "FlightStatusDb": "Server=(localdb)\\MSSQLLocalDB;Database=FlightStatusDb;Trusted_Connection=true;MultipleActiveResultSets=true"
  },
  "JwtSettings": {
    "_comment": "DEMO KEY ONLY — committed so the app runs from a clean clone. Replace with an environment variable or secrets manager in production.",
    "Secret": "SkyRoute-JWT-SuperSecret-Key-2026-AtLeast32Chars!",
    "Issuer": "FlightStatus.Api",
    "Audience": "FlightStatus.Client",
    "ExpiryMinutes": "60"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
